'use strict';exports.__esModule = true;exports.default =



refType;var _graphql = require('graphql');var types = {};function refType(field) {
  var name = field.name + 'RefType';
  if (types[name]) return types[name];

  types[name] = new _graphql.GraphQLScalarType({
    name: name,
    serialize: function serialize(value) {
      return value;
    },
    parseValue: function parseValue(value) {
      return value;
    },
    parseLiteral: function parseLiteral(value) {
      return value;
    } });


  return types[name];
}
//# sourceMappingURL=refType.js.map