'use strict';exports.__esModule = true;exports.

pickNumber = pickNumber;exports.







pickInteger = pickInteger;exports.



random = random;exports.



pickDate = pickDate;exports.




one = one;exports.



pickText = pickText;var _datetime = require('../datetime');var Datetime = _interopRequireWildcard(_datetime);function _interopRequireWildcard(obj) {if (obj && obj.__esModule) {return obj;} else {var newObj = {};if (obj != null) {for (var key in obj) {if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key];}}newObj.default = obj;return newObj;}}function pickNumber() {var max = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 65555;var min = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;var fixed = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 2;if (typeof max === 'string') max = parseInt(max, 10);if (typeof min === 'string') min = parseInt(min, 10);var num = Math.random() * (max - min) + min;return parseFloat(num.toFixed(fixed), 0);}function pickInteger() {return Math.round(pickNumber.apply(undefined, arguments));}function random(max) {return Math.ceil(Math.random() * max);}function pickDate(field) {var date = Date.now() - random(10000000000);return Datetime.format(date, field.type === 'date' ? 'yyyy-MM-dd' : 'yyyy-MM-dd hh:mm:ss');}function one(items) {return items[Math.floor(Math.random() * items.length)];}function pickText(items) {var max = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;var min = arguments[2];
  var length = max;
  if (min) {
    length = pickInteger(max, min);
  }

  var buffer = [];
  while (length--) {
    buffer.push(one(items));
  }

  return max === 1 ? buffer[0] : buffer.join('');
}
//# sourceMappingURL=pick.js.map